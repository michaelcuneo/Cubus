// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void SendChatHandler(ReducerEventContext ctx, string text);
        public event SendChatHandler? OnSendChat;

        public void SendChat(string text)
        {
            conn.InternalCallReducer(new Reducer.SendChat(text));
        }

        public bool InvokeSendChat(ReducerEventContext ctx, Reducer.SendChat args)
        {
            if (OnSendChat == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnSendChat(
                ctx,
                args.Text
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class SendChat : Reducer, IReducerArgs
        {
            [DataMember(Name = "text")]
            public string Text;

            public SendChat(string Text)
            {
                this.Text = Text;
            }

            public SendChat()
            {
                this.Text = "";
            }

            string IReducerArgs.ReducerName => "send_chat";
        }
    }
}
